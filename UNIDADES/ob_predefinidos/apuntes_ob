MODIFICADORES

[abc] coincide con a,b o c
[a-z] coincide con cualquier letra minuscula
[A-Z] coincide con cualquier letra mayuscula
[0-9] coincide con cualquier dígito
[^abc] niega el conjunto (no a,b o c)



3. usar campos privados con #

class Persona {
    #nombre; // propiedad privada
    #edad;

    constructor(nombre,edad){
        this.#nombre = nombre;
        this.#edad = edad;
    }

    getNombre() {
        return this.#nombre;
    }

    setNombre(){
        this.#nombre=nombre;
    }
}

const persona = new Persona ('Juan', 30);